<!DOCTYPE html>
<html>
<head>
<style type="text/css">
      html, body, #map-canvas { height: 100%; margin: 0; padding: 0;}
    </style>
    <script type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBiC84j3pUA5mrFcjg7vvuMCfhEzx8ppCA">
    </script>
    <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
    <script type="text/javascript">
      function initialize() {
        var mapOptions = {
          center: { lat: 44.44, lng: 26.05},
          zoom: 12
        };
          var marker;
         var map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);
 			jQuery.ajax({
               url : '/game_page_json',
               dataType : 'json',
               success : function(response) {
                       for (var i = 0; i < response.length; i++) {
                           var hint = response[i];
                           tmpLatLng = new google.maps.LatLng( hint["lat"], hint["long"]);
                            marker = new google.maps.Marker({
                              map: map,
                              position: tmpLatLng,
                              title : hint["question"],
                              animation: google.maps.Animation.BOUNCE

                           });
                         //   var infowindow = new google.maps.InfoWindow({
                         //       content: hint["question"]
                         //   });
                           
                         //  bindInfoWindow(marker, map, infowindow, '');
                            marker.setMap(map);
                            google.maps.event.addListener(marker, 'click', function() {
                                infowindow.open(map,marker);
                            });
                        }
               }
          }) ;
      }
      google.maps.event.addDomListener(window, 'load', initialize);
    </script>
</head>
<title>GeoHint</title>


<body bgcolor="#BCC6CC">


<h1>Hello, <%= @user.name %>!</h1>

<table>
  <tr>
    <td width="25%" style="padding-left:20px">
    <form action="/submit_answers" method="post" class="tr">
      <% @hints.each do |hint| %>
        <div class="tr">
          <span class="td"><%= hint.question%></span>
          <p></p>
          <span class="td" style="padding-left:20px"><input type="text" name="<%=hint.id%>" size="10" placeholder="answer"></span>
          <p></p>
        </div>
       <% end %>
       <span class="td" style="padding-left:90px"><button type="submit" > Submit </button></span>
    </form>
       
    </td>
    <td width="75%">
    <div style='width: 850px; height:550px;'>
       <div id="map-canvas"></div>
      </div>
    </td>
  </tr>
</table>
  
</body>
</html>
